. /lib/functions/uci-defaults.sh
. /lib/functions/system.sh
. /lib/functions/morse.sh

board=$(board_name)

board_config_update

hostname=$(morse_generate_default_hostname)
[ -n "$hostname" ] && ucidef_set_hostname "$hostname"

board_config_flush

exit 0
