commit d9c3e1ff328211e1ebcc88b3c07c68f0174ac829
Author: Prabha Murugesan <prabha.murugesan@morsemicro.com>
Date:   Tue Mar 31 18:48:54 2026 +1100

    AP/STA: log RSSI for auth and assoc management frames

diff --git a/src/ap/drv_callbacks.c b/src/ap/drv_callbacks.c
index 70177d22d..49eba43e1 100644
--- a/src/ap/drv_callbacks.c
+++ b/src/ap/drv_callbacks.c
@@ -1671,6 +1671,14 @@ static void hostapd_notif_auth(struct hostapd_data *hapd,
 	u8 resp_ies[2 + WLAN_AUTH_CHALLENGE_LEN];
 	size_t resp_ies_len = 0;
 
+	wpa_msg(hapd->msg_ctx, MSG_INFO,
+		"authentication: STA=" MACSTR
+		" auth_alg=%u auth_transaction=%u status_code=%u"
+		" rssi=%d (offload)",
+		MAC2STR(rx_auth->peer), rx_auth->auth_type,
+		rx_auth->auth_transaction, rx_auth->status_code,
+		rx_auth->ssi_signal);
+
 	sta = ap_get_sta(hapd, rx_auth->peer);
 	if (!sta) {
 		sta = ap_sta_add(hapd, rx_auth->peer);
@@ -2671,6 +2679,14 @@ void wpa_supplicant_event(void *ctx, enum wpa_event_type event,
 			}
 		}
 #endif /* CONFIG_IEEE80211BE */
+		if (data->assoc_info.addr) {
+			wpa_msg(hapd->msg_ctx, MSG_INFO,
+				"%sassociation request: STA=" MACSTR
+				" rssi=%d (offload)",
+				data->assoc_info.reassoc ? "re" : "",
+				MAC2STR(data->assoc_info.addr),
+				data->assoc_info.ssi_signal);
+		}
 		hostapd_notif_assoc(hapd, data->assoc_info.addr,
 				    data->assoc_info.req_ies,
 				    data->assoc_info.req_ies_len,
diff --git a/src/ap/ieee802_11.c b/src/ap/ieee802_11.c
index 9be9c48de..1736bf857 100644
--- a/src/ap/ieee802_11.c
+++ b/src/ap/ieee802_11.c
@@ -2958,14 +2958,14 @@ static void handle_auth(struct hostapd_data *hapd,
 	    mgmt->u.auth.variable[1] == WLAN_AUTH_CHALLENGE_LEN)
 		challenge = &mgmt->u.auth.variable[2];
 
-	wpa_printf(MSG_DEBUG, "authentication: STA=" MACSTR " auth_alg=%d "
+	wpa_printf(MSG_INFO, "authentication: STA=" MACSTR " auth_alg=%d "
 		   "auth_transaction=%d status_code=%d wep=%d%s "
-		   "seq_ctrl=0x%x%s%s",
+		   "seq_ctrl=0x%x%s%s rssi=%d",
 		   MAC2STR(sa), auth_alg, auth_transaction,
 		   status_code, !!(fc & WLAN_FC_ISWEP),
 		   challenge ? " challenge" : "",
 		   seq_ctrl, (fc & WLAN_FC_RETRY) ? " retry" : "",
-		   from_queue ? " (from queue)" : "");
+		   from_queue ? " (from queue)" : "", rssi);
 
 #ifdef CONFIG_NO_RC4
 	if (auth_alg == WLAN_AUTH_SHARED_KEY) {
@@ -5584,23 +5584,25 @@ static void handle_assoc(struct hostapd_data *hapd,
 		capab_info = le_to_host16(mgmt->u.reassoc_req.capab_info);
 		listen_interval = le_to_host16(
 			mgmt->u.reassoc_req.listen_interval);
-		wpa_printf(MSG_DEBUG, "reassociation request: STA=" MACSTR
+		wpa_printf(MSG_INFO, "reassociation request: STA=" MACSTR
 			   " capab_info=0x%02x listen_interval=%d current_ap="
-			   MACSTR " seq_ctrl=0x%x%s",
+			   MACSTR " seq_ctrl=0x%x%s rssi=%d",
 			   MAC2STR(mgmt->sa), capab_info, listen_interval,
 			   MAC2STR(mgmt->u.reassoc_req.current_ap),
-			   seq_ctrl, (fc & WLAN_FC_RETRY) ? " retry" : "");
+			   seq_ctrl, (fc & WLAN_FC_RETRY) ? " retry" : "",
+			   rssi);
 		left = len - (IEEE80211_HDRLEN + sizeof(mgmt->u.reassoc_req));
 		pos = mgmt->u.reassoc_req.variable;
 	} else {
 		capab_info = le_to_host16(mgmt->u.assoc_req.capab_info);
 		listen_interval = le_to_host16(
 			mgmt->u.assoc_req.listen_interval);
-		wpa_printf(MSG_DEBUG, "association request: STA=" MACSTR
+		wpa_printf(MSG_INFO, "association request: STA=" MACSTR
 			   " capab_info=0x%02x listen_interval=%d "
-			   "seq_ctrl=0x%x%s",
+			   "seq_ctrl=0x%x%s rssi=%d",
 			   MAC2STR(mgmt->sa), capab_info, listen_interval,
-			   seq_ctrl, (fc & WLAN_FC_RETRY) ? " retry" : "");
+			   seq_ctrl, (fc & WLAN_FC_RETRY) ? " retry" : "",
+			   rssi);
 		left = len - (IEEE80211_HDRLEN + sizeof(mgmt->u.assoc_req));
 		pos = mgmt->u.assoc_req.variable;
 	}
diff --git a/src/drivers/driver.h b/src/drivers/driver.h
index fa016c1a6..47d2bf48b 100644
--- a/src/drivers/driver.h
+++ b/src/drivers/driver.h
@@ -6186,6 +6186,14 @@ union wpa_event_data {
 		 */
 		unsigned int freq;
 
+		/**
+		 * ssi_signal - Signal strength in dBm of the received
+		 *	association frame ((Re)Association Response in STA
+		 *	mode or (Re)Association Request in AP mode/offload)
+		 *	(or 0 if not available)
+		 */
+		int ssi_signal;
+
 		/**
 		 * wmm_params - WMM parameters used in this association.
 		 */
@@ -6456,6 +6464,7 @@ union wpa_event_data {
 		u16 status_code;
 		const u8 *ies;
 		size_t ies_len;
+		int ssi_signal;
 	} auth;
 
 	/**
@@ -6511,6 +6520,12 @@ union wpa_event_data {
 		 * reason_code - Connection failure reason code from the driver
 		 */
 		enum sta_connect_fail_reason_codes reason_code;
+
+		/**
+		 * ssi_signal - Signal strength in dBm of the received
+		 *	(Re)Association Response frame (or 0 if not available)
+		 */
+		int ssi_signal;
 	} assoc_reject;
 
 	struct timeout_event {
diff --git a/src/drivers/driver_nl80211_event.c b/src/drivers/driver_nl80211_event.c
index 10d1452dc..d6e4394cc 100644
--- a/src/drivers/driver_nl80211_event.c
+++ b/src/drivers/driver_nl80211_event.c
@@ -198,10 +198,12 @@ static const char * nl80211_command_to_string(enum nl80211_commands cmd)
 
 
 static void mlme_event_auth(struct wpa_driver_nl80211_data *drv,
-			    const u8 *frame, size_t len)
+			    const u8 *frame, size_t len,
+			    struct nlattr *sig)
 {
 	const struct ieee80211_mgmt *mgmt;
 	union wpa_event_data event;
+	int ssi_signal = 0;
 
 	if (!(drv->capa.flags & WPA_DRIVER_FLAGS_SME) &&
 	    drv->force_connect_cmd) {
@@ -222,6 +224,9 @@ static void mlme_event_auth(struct wpa_driver_nl80211_data *drv,
 		return;
 	}
 
+	if (sig)
+		ssi_signal = (s32) nla_get_u32(sig);
+
 	os_memcpy(drv->auth_bssid, mgmt->sa, ETH_ALEN);
 	os_memset(drv->auth_attempt_bssid, 0, ETH_ALEN);
 	os_memset(&event, 0, sizeof(event));
@@ -234,6 +239,7 @@ static void mlme_event_auth(struct wpa_driver_nl80211_data *drv,
 		event.auth.ies = mgmt->u.auth.variable;
 		event.auth.ies_len = len - 24 - sizeof(mgmt->u.auth);
 	}
+	event.auth.ssi_signal = ssi_signal;
 
 	wpa_supplicant_event(drv->ctx, EVENT_AUTH, &event);
 }
@@ -260,13 +266,14 @@ static void nl80211_parse_wmm_params(struct nlattr *wmm_attr,
 
 
 static void mlme_event_assoc(struct wpa_driver_nl80211_data *drv,
-			     const u8 *frame, size_t len, struct nlattr *wmm,
-			     struct nlattr *req_ie)
+			     const u8 *frame, size_t len, struct nlattr *sig,
+			     struct nlattr *wmm, struct nlattr *req_ie)
 {
 	const struct ieee80211_mgmt *mgmt;
 	union wpa_event_data event;
 	u16 status;
 	int ssid_len;
+	int ssi_signal = 0;
 
 	if (!(drv->capa.flags & WPA_DRIVER_FLAGS_SME) &&
 	    drv->force_connect_cmd) {
@@ -287,6 +294,9 @@ static void mlme_event_assoc(struct wpa_driver_nl80211_data *drv,
 		return;
 	}
 
+	if (sig)
+		ssi_signal = (s32) nla_get_u32(sig);
+
 	status = le_to_host16(mgmt->u.assoc_resp.status_code);
 	if (status != WLAN_STATUS_SUCCESS) {
 		os_memset(&event, 0, sizeof(event));
@@ -298,6 +308,7 @@ static void mlme_event_assoc(struct wpa_driver_nl80211_data *drv,
 				len - 24 - sizeof(mgmt->u.assoc_resp);
 		}
 		event.assoc_reject.status_code = status;
+		event.assoc_reject.ssi_signal = ssi_signal;
 
 		wpa_supplicant_event(drv->ctx, EVENT_ASSOC_REJECT, &event);
 		return;
@@ -315,6 +326,7 @@ static void mlme_event_assoc(struct wpa_driver_nl80211_data *drv,
 		event.assoc_info.resp_ies_len =
 			len - 24 - sizeof(mgmt->u.assoc_resp);
 	}
+	event.assoc_info.ssi_signal = ssi_signal;
 
 	if (req_ie) {
 		event.assoc_info.req_ies = nla_data(req_ie);
@@ -909,6 +921,7 @@ static void mlme_event_connect(struct wpa_driver_nl80211_data *drv,
 			       struct nlattr *status,
 			       struct nlattr *addr, struct nlattr *req_ie,
 			       struct nlattr *resp_ie,
+			       struct nlattr *sig,
 			       struct nlattr *timed_out,
 			       struct nlattr *timeout_reason,
 			       struct nlattr *authorized,
@@ -925,6 +938,7 @@ static void mlme_event_connect(struct wpa_driver_nl80211_data *drv,
 	const u8 *ssid = NULL;
 	u16 status_code;
 	int ssid_len;
+	int ssi_signal = 0;
 
 	if (drv->capa.flags & WPA_DRIVER_FLAGS_SME) {
 		/*
@@ -939,6 +953,8 @@ static void mlme_event_connect(struct wpa_driver_nl80211_data *drv,
 	drv->connect_reassoc = 0;
 
 	status_code = status ? nla_get_u16(status) : WLAN_STATUS_SUCCESS;
+	if (sig)
+		ssi_signal = (s32) nla_get_u32(sig);
 
 	if (cmd == NL80211_CMD_CONNECT) {
 		wpa_printf(MSG_DEBUG,
@@ -972,6 +988,7 @@ static void mlme_event_connect(struct wpa_driver_nl80211_data *drv,
 			event.assoc_reject.resp_ies_len = nla_len(resp_ie);
 		}
 		event.assoc_reject.status_code = status_code;
+		event.assoc_reject.ssi_signal = ssi_signal;
 		event.assoc_reject.timed_out = timed_out != NULL;
 		if (timed_out && timeout_reason) {
 			enum nl80211_timeout_reason reason;
@@ -1040,6 +1057,7 @@ static void mlme_event_connect(struct wpa_driver_nl80211_data *drv,
 		event.assoc_info.resp_ies = nla_data(resp_ie);
 		event.assoc_info.resp_ies_len = nla_len(resp_ie);
 	}
+	event.assoc_info.ssi_signal = ssi_signal;
 
 	event.assoc_info.freq = nl80211_get_assoc_freq(drv);
 	drv->first_bss->flink->freq = drv->assoc_freq;
@@ -1744,11 +1762,11 @@ static void mlme_event(struct i802_bss *bss,
 
 	switch (cmd) {
 	case NL80211_CMD_AUTHENTICATE:
-		mlme_event_auth(drv, nla_data(frame), nla_len(frame));
+		mlme_event_auth(drv, nla_data(frame), nla_len(frame), sig);
 		break;
 	case NL80211_CMD_ASSOCIATE:
-		mlme_event_assoc(drv, nla_data(frame), nla_len(frame), wmm,
-				 req_ie);
+		mlme_event_assoc(drv, nla_data(frame), nla_len(frame), sig,
+				 wmm, req_ie);
 		break;
 	case NL80211_CMD_DEAUTHENTICATE:
 		mlme_event_deauth_disassoc(drv, EVENT_DEAUTH,
@@ -2858,6 +2876,7 @@ static void qca_nl80211_key_mgmt_auth(struct wpa_driver_nl80211_data *drv,
 			   tb[QCA_WLAN_VENDOR_ATTR_ROAM_AUTH_BSSID],
 			   tb[QCA_WLAN_VENDOR_ATTR_ROAM_AUTH_REQ_IE],
 			   tb[QCA_WLAN_VENDOR_ATTR_ROAM_AUTH_RESP_IE],
+			   NULL,
 			   NULL, NULL,
 			   tb[QCA_WLAN_VENDOR_ATTR_ROAM_AUTH_AUTHORIZED],
 			   tb[QCA_WLAN_VENDOR_ATTR_ROAM_AUTH_KEY_REPLAY_CTR],
@@ -4052,6 +4071,7 @@ static void do_process_drv_event(struct i802_bss *bss, int cmd,
 				   tb[NL80211_ATTR_MAC],
 				   tb[NL80211_ATTR_REQ_IE],
 				   tb[NL80211_ATTR_RESP_IE],
+				   tb[NL80211_ATTR_RX_SIGNAL_DBM],
 				   tb[NL80211_ATTR_TIMED_OUT],
 				   tb[NL80211_ATTR_TIMEOUT_REASON],
 				   NULL, NULL, NULL,
diff --git a/wpa_supplicant/events.c b/wpa_supplicant/events.c
index f6ca645f4..d4f13724a 100644
--- a/wpa_supplicant/events.c
+++ b/wpa_supplicant/events.c
@@ -4420,7 +4420,9 @@ static void wpa_supplicant_event_assoc(struct wpa_supplicant *wpa_s,
 	wpa_s->sme.last_unprot_disconnect.sec = 0;
 #endif /* CONFIG_SME */
 
-	wpa_msg(wpa_s, MSG_INFO, "Associated with " MACSTR, MAC2STR(bssid));
+	wpa_msg(wpa_s, MSG_INFO, "Associated with " MACSTR " rssi=%d",
+		MAC2STR(bssid),
+		wpa_s->current_bss ? wpa_s->current_bss->level : 0);
 	if (wpa_s->current_ssid) {
 		/* When using scanning (ap_scan=1), SIM PC/SC interface can be
 		 * initialized before association, but for other modes,
@@ -5864,6 +5866,7 @@ static void wpas_event_assoc_reject(struct wpa_supplicant *wpa_s,
 				    union wpa_event_data *data)
 {
 	const u8 *bssid = data->assoc_reject.bssid;
+	int rssi;
 	struct ieee802_11_elems elems;
 	struct ml_sta_link_info ml_info[MAX_NUM_MLD_LINKS];
 	const u8 *link_bssids[MAX_NUM_MLD_LINKS + 1];
@@ -5873,6 +5876,7 @@ static void wpas_event_assoc_reject(struct wpa_supplicant *wpa_s,
 
 	if (!bssid || is_zero_ether_addr(bssid))
 		bssid = wpa_s->pending_bssid;
+	rssi = wpa_s->current_bss ? wpa_s->current_bss->level : 0;
 #ifdef CONFIG_MBO
 	if (wpa_s->drv_flags & WPA_DRIVER_FLAGS_SME)
 		reject_bss = wpa_s->current_bss;
@@ -5882,7 +5886,8 @@ static void wpas_event_assoc_reject(struct wpa_supplicant *wpa_s,
 
 	if (data->assoc_reject.bssid)
 		wpa_msg(wpa_s, MSG_INFO, WPA_EVENT_ASSOC_REJECT
-			"bssid=" MACSTR	" status_code=%u%s%s%s%s%s",
+			"bssid=" MACSTR	" status_code=%u%s%s%s%s%s"
+			" rssi=%d",
 			MAC2STR(data->assoc_reject.bssid),
 			data->assoc_reject.status_code,
 			data->assoc_reject.timed_out ? " timeout" : "",
@@ -5892,10 +5897,11 @@ static void wpas_event_assoc_reject(struct wpa_supplicant *wpa_s,
 			data->assoc_reject.reason_code !=
 			STA_CONNECT_FAIL_REASON_UNSPECIFIED ?
 			" qca_driver_reason=" : "",
-			connect_fail_reason(data->assoc_reject.reason_code));
+			connect_fail_reason(data->assoc_reject.reason_code),
+			rssi);
 	else
 		wpa_msg(wpa_s, MSG_INFO, WPA_EVENT_ASSOC_REJECT
-			"status_code=%u%s%s%s%s%s",
+			"status_code=%u%s%s%s%s%s rssi=%d",
 			data->assoc_reject.status_code,
 			data->assoc_reject.timed_out ? " timeout" : "",
 			data->assoc_reject.timeout_reason ? "=" : "",
@@ -5904,7 +5910,8 @@ static void wpas_event_assoc_reject(struct wpa_supplicant *wpa_s,
 			data->assoc_reject.reason_code !=
 			STA_CONNECT_FAIL_REASON_UNSPECIFIED ?
 			" qca_driver_reason=" : "",
-			connect_fail_reason(data->assoc_reject.reason_code));
+			connect_fail_reason(data->assoc_reject.reason_code),
+			rssi);
 	wpa_s->assoc_status_code = data->assoc_reject.status_code;
 	wpas_notify_assoc_status_code(wpa_s);
 
diff --git a/wpa_supplicant/sme.c b/wpa_supplicant/sme.c
index 1f0e05c77..341bb2116 100644
--- a/wpa_supplicant/sme.c
+++ b/wpa_supplicant/sme.c
@@ -1688,7 +1688,8 @@ static int sme_external_ml_auth(struct wpa_supplicant *wpa_s,
 
 static int sme_sae_auth(struct wpa_supplicant *wpa_s, u16 auth_transaction,
 			u16 status_code, const u8 *data, size_t len,
-			int external, const u8 *sa, int *ie_offset)
+			int external, const u8 *sa, int *ie_offset,
+			int ssi_signal)
 {
 	int *groups;
 
@@ -1846,9 +1847,10 @@ static int sme_sae_auth(struct wpa_supplicant *wpa_s, u16 auth_transaction,
 		const u8 *bssid = sa ? sa : wpa_s->pending_bssid;
 
 		wpa_msg(wpa_s, MSG_INFO, WPA_EVENT_AUTH_REJECT MACSTR
-			" auth_type=%u auth_transaction=%u status_code=%u",
+			" auth_type=%u auth_transaction=%u status_code=%u"
+			" rssi=%d",
 			MAC2STR(bssid), WLAN_AUTH_SAE,
-			auth_transaction, status_code);
+			auth_transaction, status_code, ssi_signal);
 		return -2;
 	}
 
@@ -2012,7 +2014,7 @@ void sme_external_auth_mgmt_rx(struct wpa_supplicant *wpa_s,
 			wpa_s, le_to_host16(header->u.auth.auth_transaction),
 			le_to_host16(header->u.auth.status_code),
 			header->u.auth.variable,
-			len - auth_length, 1, header->sa, &ie_offset);
+			len - auth_length, 1, header->sa, &ie_offset, 0);
 		if (res < 0) {
 			/* Notify failure to the driver */
 			sme_send_external_auth_status(
@@ -2045,6 +2047,7 @@ void sme_event_auth(struct wpa_supplicant *wpa_s, union wpa_event_data *data)
 {
 	struct wpa_ssid *ssid = wpa_s->current_ssid;
 	int ie_offset = 0;
+	int rssi;
 
 	if (ssid == NULL) {
 		wpa_dbg(wpa_s, MSG_DEBUG, "SME: Ignore authentication event "
@@ -2067,10 +2070,13 @@ void sme_event_auth(struct wpa_supplicant *wpa_s, union wpa_event_data *data)
 		return;
 	}
 
-	wpa_dbg(wpa_s, MSG_DEBUG, "SME: Authentication response: peer=" MACSTR
-		" auth_type=%d auth_transaction=%d status_code=%d",
+	rssi = wpa_s->current_bss ? wpa_s->current_bss->level : 0;
+
+	wpa_msg(wpa_s, MSG_INFO, "SME: Authentication response: peer=" MACSTR
+		" auth_type=%d auth_transaction=%d status_code=%d rssi=%d",
 		MAC2STR(data->auth.peer), data->auth.auth_type,
-		data->auth.auth_transaction, data->auth.status_code);
+		data->auth.auth_transaction, data->auth.status_code,
+		rssi);
 	wpa_hexdump(MSG_MSGDUMP, "SME: Authentication response IEs",
 		    data->auth.ies, data->auth.ies_len);
 
@@ -2084,7 +2090,7 @@ void sme_event_auth(struct wpa_supplicant *wpa_s, union wpa_event_data *data)
 		res = sme_sae_auth(wpa_s, data->auth.auth_transaction,
 				   data->auth.status_code, data->auth.ies,
 				   data->auth.ies_len, 0, data->auth.peer,
-				   &ie_offset);
+				   &ie_offset, rssi);
 		if (res < 0) {
 			wpas_connection_failed(wpa_s, wpa_s->pending_bssid,
 					       NULL);
@@ -2121,11 +2127,13 @@ void sme_event_auth(struct wpa_supplicant *wpa_s, union wpa_event_data *data)
 			}
 		}
 		wpa_msg(wpa_s, MSG_INFO, WPA_EVENT_AUTH_REJECT MACSTR
-			" auth_type=%u auth_transaction=%u status_code=%u%s%s",
+			" auth_type=%u auth_transaction=%u status_code=%u%s%s"
+			" rssi=%d",
 			MAC2STR(data->auth.peer), data->auth.auth_type,
 			data->auth.auth_transaction, data->auth.status_code,
 			ie_txt ? " ie=" : "",
-			ie_txt ? ie_txt : "");
+			ie_txt ? ie_txt : "",
+			rssi);
 		os_free(ie_txt);
 
 #ifdef CONFIG_FILS
@@ -2956,14 +2964,18 @@ void sme_event_assoc_reject(struct wpa_supplicant *wpa_s,
 {
 	const u8 *bssid;
 
+	int rssi;
+
 	if (wpa_s->valid_links)
 		bssid = wpa_s->ap_mld_addr;
 	else
 		bssid = wpa_s->pending_bssid;
 
-	wpa_dbg(wpa_s, MSG_DEBUG, "SME: Association with " MACSTR " failed: "
-		"status code %d", MAC2STR(wpa_s->pending_bssid),
-		data->assoc_reject.status_code);
+	rssi = wpa_s->current_bss ? wpa_s->current_bss->level : 0;
+
+	wpa_msg(wpa_s, MSG_INFO, "SME: Association with " MACSTR " failed: "
+		"status code %d rssi=%d", MAC2STR(wpa_s->pending_bssid),
+		data->assoc_reject.status_code, rssi);
 
 	eloop_cancel_timeout(sme_assoc_timer, wpa_s, NULL);
 	eloop_cancel_timeout(sme_assoc_comeback_timer, wpa_s, NULL);
