From ac502a9907d6ffbd2a39627ec72c42a626a83d1c Mon Sep 17 00:00:00 2001
From: syang <samuele.yang@morsemicro.com>
Date: Tue, 5 May 2026 12:14:20 +1000
Subject: [PATCH] Forward patch for 6c3bf0c

---
 essentials/hostapd_s1g/src/src/ap/ubus.c      | 353 +++++++++++++++++-
 essentials/hostapd_s1g/src/src/ap/ubus.h      |  19 +-
 .../hostapd_s1g/src/wpa_supplicant/ubus.c     |  45 ++-
 .../hostapd_s1g/src/wpa_supplicant/ubus.h     |  13 +-
 4 files changed, 407 insertions(+), 23 deletions(-)

diff --git a/essentials/hostapd_s1g/src/src/ap/ubus.c b/essentials/hostapd_s1g/src/src/ap/ubus.c
index 7d602e1b..572603db 100644
--- a/src/ap/ubus.c
+++ b/src/ap/ubus.c
@@ -10,6 +10,7 @@
 #include "utils/common.h"
 #include "utils/eloop.h"
 #include "utils/wpabuf.h"
+#include "common/dpp.h"
 #include "common/ieee802_11_defs.h"
 #include "common/hw_features_common.h"
 #include "hostapd.h"
@@ -789,6 +790,318 @@ hostapd_bss_wps_cancel(struct ubus_context *ctx, struct ubus_object *obj,
 }
 #endif /* CONFIG_WPS */
 
+#ifdef CONFIG_DPP
+#define HOSTAPD_UBUS_DPP_REPLY_LEN 4096
+
+static const char *
+hostapd_ubus_dpp_get_id_arg(struct blob_attr *attr, const char *default_value)
+{
+	const char *id;
+
+	if (!attr)
+		return default_value;
+
+	if (blobmsg_type(attr) != BLOBMSG_TYPE_STRING)
+		return NULL;
+
+	id = blobmsg_get_string(attr);
+	if (!id || !*id)
+		return NULL;
+
+	return id;
+}
+
+static int
+hostapd_ubus_dpp_validate_id_arg(const char *id)
+{
+	const char *pos;
+
+	if (!id || !*id)
+		return -1;
+
+	for (pos = id; *pos; pos++) {
+		if (*pos < '0' || *pos > '9')
+			return -1;
+	}
+
+	return 0;
+}
+
+static int
+hostapd_ubus_dpp_parse_id(const char *id, unsigned int *value)
+{
+	unsigned long parsed;
+
+	if (hostapd_ubus_dpp_validate_id_arg(id) < 0)
+		return -1;
+
+	parsed = strtoul(id, NULL, 10);
+	if ((unsigned int) parsed != parsed)
+		return -1;
+
+	*value = (unsigned int) parsed;
+	return 0;
+}
+
+enum {
+	DPP_QR_CODE_URI,
+	__DPP_QR_CODE_MAX
+};
+
+static const struct blobmsg_policy dpp_qr_code_policy[__DPP_QR_CODE_MAX] = {
+	[DPP_QR_CODE_URI] = { "uri", BLOBMSG_TYPE_STRING },
+};
+
+static int
+hostapd_bss_dpp_qr_code(struct ubus_context *ctx, struct ubus_object *obj,
+			struct ubus_request_data *req, const char *method,
+			struct blob_attr *msg)
+{
+	int rc;
+	struct blob_attr *tb[__DPP_QR_CODE_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+	const char *uri;
+
+	if (msg) {
+		blobmsg_parse(dpp_qr_code_policy, __DPP_QR_CODE_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	uri = tb[DPP_QR_CODE_URI] ? blobmsg_data(tb[DPP_QR_CODE_URI]) : NULL;
+
+	if (!uri || !*uri)
+		return UBUS_STATUS_INVALID_ARGUMENT;
+
+	rc = hostapd_dpp_qr_code(hapd, uri);
+	if (rc < 0)
+		return UBUS_STATUS_NOT_SUPPORTED;
+
+	blob_buf_init(&b, 0);
+	blobmsg_add_u32(&b, "id", rc);
+	ubus_send_reply(ctx, req, b.head);
+
+	return 0;
+}
+
+enum {
+	DPP_ID,
+	__DPP_ID_MAX
+};
+
+static const struct blobmsg_policy dpp_id_policy[__DPP_ID_MAX] = {
+	[DPP_ID] = { "id", BLOBMSG_TYPE_STRING },
+};
+
+static int
+hostapd_bss_dpp_bootstrap_remove(struct ubus_context *ctx,
+				 struct ubus_object *obj,
+				 struct ubus_request_data *req,
+				 const char *method, struct blob_attr *msg)
+{
+	const char *arg;
+	struct blob_attr *tb[__DPP_ID_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+
+	if (msg) {
+		blobmsg_parse(dpp_id_policy, __DPP_ID_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	arg = hostapd_ubus_dpp_get_id_arg(tb[DPP_ID], "1");
+	if (hostapd_ubus_dpp_validate_id_arg(arg) < 0)
+		return UBUS_STATUS_INVALID_ARGUMENT;
+
+	if (dpp_bootstrap_remove(hapd->iface->interfaces->dpp, arg) < 0)
+		return UBUS_STATUS_NOT_SUPPORTED;
+
+	return 0;
+}
+
+static int
+hostapd_bss_dpp_configurator_get_key(struct ubus_context *ctx,
+				     struct ubus_object *obj,
+				     struct ubus_request_data *req,
+				     const char *method, struct blob_attr *msg)
+{
+	int rc;
+	unsigned int id;
+	const char *arg;
+	char key[HOSTAPD_UBUS_DPP_REPLY_LEN];
+	struct blob_attr *tb[__DPP_ID_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+
+	if (msg) {
+		blobmsg_parse(dpp_id_policy, __DPP_ID_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	arg = hostapd_ubus_dpp_get_id_arg(tb[DPP_ID], "1");
+	if (hostapd_ubus_dpp_parse_id(arg, &id) < 0)
+		return UBUS_STATUS_INVALID_ARGUMENT;
+
+	rc = dpp_configurator_get_key_id(hapd->iface->interfaces->dpp, id,
+					 key, sizeof(key));
+	if (rc < 0)
+		return UBUS_STATUS_NOT_SUPPORTED;
+
+	blob_buf_init(&b, 0);
+	blobmsg_add_u32(&b, "id", id);
+	blobmsg_add_string(&b, "key", key);
+	ubus_send_reply(ctx, req, b.head);
+
+	return 0;
+}
+
+enum {
+	DPP_CONFIGURATOR_ADD_CRYPTO_CONF,
+	__DPP_CONFIGURATOR_ADD_MAX
+};
+
+static const struct blobmsg_policy
+dpp_configurator_add_policy[__DPP_CONFIGURATOR_ADD_MAX] = {
+	[DPP_CONFIGURATOR_ADD_CRYPTO_CONF] = {
+		"crypto_conf", BLOBMSG_TYPE_STRING
+	},
+};
+
+static int
+hostapd_bss_dpp_configurator_add(struct ubus_context *ctx,
+				 struct ubus_object *obj,
+				 struct ubus_request_data *req,
+				 const char *method, struct blob_attr *msg)
+{
+	int rc;
+	int len;
+	char *cmd_buf = NULL;
+	const char *cmd = "";
+	const char *crypto_conf;
+	struct blob_attr *tb[__DPP_CONFIGURATOR_ADD_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+
+	if (msg) {
+		blobmsg_parse(dpp_configurator_add_policy,
+			      __DPP_CONFIGURATOR_ADD_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	crypto_conf = tb[DPP_CONFIGURATOR_ADD_CRYPTO_CONF] ?
+		blobmsg_get_string(tb[DPP_CONFIGURATOR_ADD_CRYPTO_CONF]) : NULL;
+	if (crypto_conf && *crypto_conf) {
+		if (crypto_conf[0] == ' ') {
+			cmd = crypto_conf;
+		} else {
+			cmd_buf = os_malloc(os_strlen(crypto_conf) + 2);
+			if (!cmd_buf)
+				return UBUS_STATUS_UNKNOWN_ERROR;
+
+			len = os_snprintf(cmd_buf, os_strlen(crypto_conf) + 2,
+					  " %s", crypto_conf);
+			if (os_snprintf_error(os_strlen(crypto_conf) + 2, len)) {
+				os_free(cmd_buf);
+				return UBUS_STATUS_UNKNOWN_ERROR;
+			}
+
+			cmd = cmd_buf;
+		}
+	}
+
+	rc = dpp_configurator_add(hapd->iface->interfaces->dpp, cmd);
+	os_free(cmd_buf);
+	if (rc < 0)
+		return UBUS_STATUS_NOT_SUPPORTED;
+
+	blob_buf_init(&b, 0);
+	blobmsg_add_u32(&b, "id", rc);
+	ubus_send_reply(ctx, req, b.head);
+
+	return 0;
+}
+
+enum {
+	DPP_CONFIGURATOR_PARAMS_DPP_CONF,
+	__DPP_CONFIGURATOR_PARAMS_MAX
+};
+
+static const struct blobmsg_policy
+dpp_configurator_params_policy[__DPP_CONFIGURATOR_PARAMS_MAX] = {
+	[DPP_CONFIGURATOR_PARAMS_DPP_CONF] = { "dpp_conf", BLOBMSG_TYPE_STRING },
+};
+
+static int
+hostapd_bss_set_dpp_configurator_params(struct ubus_context *ctx,
+					struct ubus_object *obj,
+					struct ubus_request_data *req,
+					const char *method,
+					struct blob_attr *msg)
+{
+	char *params;
+	const char *dpp_conf;
+	struct blob_attr *tb[__DPP_CONFIGURATOR_PARAMS_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+
+	if (msg) {
+		blobmsg_parse(dpp_configurator_params_policy,
+			      __DPP_CONFIGURATOR_PARAMS_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	dpp_conf = tb[DPP_CONFIGURATOR_PARAMS_DPP_CONF] ?
+		blobmsg_get_string(tb[DPP_CONFIGURATOR_PARAMS_DPP_CONF]) : NULL;
+
+	if (!dpp_conf || !*dpp_conf)
+		return UBUS_STATUS_INVALID_ARGUMENT;
+
+	params = os_strdup(dpp_conf);
+	if (!params)
+		return UBUS_STATUS_UNKNOWN_ERROR;
+
+	os_free(hapd->dpp_configurator_params);
+	hapd->dpp_configurator_params = params;
+#ifdef CONFIG_DPP2
+	dpp_controller_set_params(hapd->iface->interfaces->dpp, params);
+#endif /* CONFIG_DPP2 */
+
+	return 0;
+}
+
+static int
+hostapd_bss_dpp_configurator_remove(struct ubus_context *ctx,
+				    struct ubus_object *obj,
+				    struct ubus_request_data *req,
+				    const char *method, struct blob_attr *msg)
+{
+	const char *arg;
+	struct blob_attr *tb[__DPP_ID_MAX];
+	struct hostapd_data *hapd = get_hapd_from_object(obj);
+
+	if (msg) {
+		blobmsg_parse(dpp_id_policy, __DPP_ID_MAX, tb,
+			      blob_data(msg), blob_len(msg));
+	} else {
+		memset(tb, 0, sizeof(tb));
+	}
+
+	arg = hostapd_ubus_dpp_get_id_arg(tb[DPP_ID], "1");
+	if (hostapd_ubus_dpp_validate_id_arg(arg) < 0)
+		return UBUS_STATUS_INVALID_ARGUMENT;
+
+	if (dpp_configurator_remove(hapd->iface->interfaces->dpp, arg) < 0)
+		return UBUS_STATUS_NOT_SUPPORTED;
+
+	return 0;
+}
+#endif /* CONFIG_DPP */
+
 #ifdef CONFIG_DPP3
 enum {
 	DPP_PUSH_BUTTON_COMMAND,
@@ -1827,6 +2140,14 @@ static const struct ubus_method bss_methods[] = {
 	UBUS_METHOD_NOARG("wps_status", hostapd_bss_wps_status),
 	UBUS_METHOD_NOARG("wps_cancel", hostapd_bss_wps_cancel),
 #endif
+#ifdef CONFIG_DPP
+	UBUS_METHOD("dpp_qr_code", hostapd_bss_dpp_qr_code, dpp_qr_code_policy),
+	UBUS_METHOD("dpp_bootstrap_remove", hostapd_bss_dpp_bootstrap_remove, dpp_id_policy),
+	UBUS_METHOD("dpp_configurator_get_key", hostapd_bss_dpp_configurator_get_key, dpp_id_policy),
+	UBUS_METHOD("dpp_configurator_add", hostapd_bss_dpp_configurator_add, dpp_configurator_add_policy),
+	UBUS_METHOD("set_dpp_configurator_params", hostapd_bss_set_dpp_configurator_params, dpp_configurator_params_policy),
+	UBUS_METHOD("dpp_configurator_remove", hostapd_bss_dpp_configurator_remove, dpp_id_policy),
+#endif
 #ifdef CONFIG_DPP3
 	UBUS_METHOD("dpp_push_button", hostapd_bss_dpp_push_button, dpp_push_button_policy),
 #endif
@@ -2055,25 +2376,45 @@ void hostapd_ubus_notify_type(struct hostapd_data *hapd, const char *type)
 }
 
 #ifdef CONFIG_DPP
-void hostapd_ubus_notify_dpp_conf_failed(struct hostapd_data *hapd)
+static bool hostapd_ubus_dpp_is_qrcode(const struct dpp_authentication *auth)
+{
+	if (!auth)
+		return false;
+
+	return (auth->own_bi && auth->own_bi->type == DPP_BOOTSTRAP_QR_CODE) ||
+		(auth->peer_bi && auth->peer_bi->type == DPP_BOOTSTRAP_QR_CODE);
+}
+
+void hostapd_ubus_notify_dpp_conf_failed(struct hostapd_data *hapd,
+					 const struct dpp_authentication *auth)
 {
 	blob_buf_init(&b, 0);
 
 	blobmsg_add_string(&b, "ifname", hapd->conf->iface);
+	if (hostapd_ubus_dpp_is_qrcode(auth))
+		blobmsg_add_macaddr(&b, "peer_mac", auth->peer_mac_addr);
 
-	ubus_send_event(ctx, "dpp.conf_failed", b.head);
+	ubus_send_event(ctx,
+			hostapd_ubus_dpp_is_qrcode(auth) ?
+			"dpp.qr.conf_failed" : "dpp.pb.conf_failed",
+			b.head);
 }
-#endif
 
-#ifdef CONFIG_DPP3
-void hostapd_ubus_notify_dpp_pb_result(struct hostapd_data *hapd, const char *status)
+void hostapd_ubus_notify_dpp_result(struct hostapd_data *hapd,
+				    const char *status,
+				    const struct dpp_authentication *auth)
 {
 	blob_buf_init(&b, 0);
 
 	blobmsg_add_string(&b, "ifname", hapd->conf->iface);
 	blobmsg_add_string(&b, "status", status);
+	if (hostapd_ubus_dpp_is_qrcode(auth))
+		blobmsg_add_macaddr(&b, "peer_mac", auth->peer_mac_addr);
 
-	ubus_send_event(ctx, "dpp.result", b.head);
+	ubus_send_event(ctx,
+			hostapd_ubus_dpp_is_qrcode(auth) ?
+			"dpp.qr.result" : "dpp.pb.result",
+			b.head);
 }
 #endif
 
diff --git a/essentials/hostapd_s1g/src/src/ap/ubus.h b/essentials/hostapd_s1g/src/src/ap/ubus.h
index 9ac8875b..4cde753f 100644
--- a/src/ap/ubus.h
+++ b/src/ap/ubus.h
@@ -28,6 +28,7 @@ struct hostapd_data;
 struct sta_info;
 struct hapd_interfaces;
 struct rrm_measurement_beacon_report;
+struct dpp_authentication;
 
 #ifdef UBUS_SUPPORT
 
@@ -54,10 +55,11 @@ void hostapd_ubus_handle_link_measurement(struct hostapd_data *hapd, const u8 *d
 void hostapd_ubus_notify_type(struct hostapd_data *hapd, const char *type);
 void hostapd_ubus_notify(struct hostapd_data *hapd, const char *type, const u8 *mac);
 #ifdef CONFIG_DPP
-void hostapd_ubus_notify_dpp_conf_failed(struct hostapd_data *hapd);
-#endif
-#ifdef CONFIG_DPP3
-void hostapd_ubus_notify_dpp_pb_result(struct hostapd_data *hapd, const char *status);
+void hostapd_ubus_notify_dpp_conf_failed(
+	struct hostapd_data *hapd, const struct dpp_authentication *auth);
+void hostapd_ubus_notify_dpp_result(struct hostapd_data *hapd,
+				    const char *status,
+				    const struct dpp_authentication *auth);
 #endif
 void hostapd_ubus_notify_beacon_report(struct hostapd_data *hapd,
 				       const u8 *addr, u8 token, u8 rep_mode,
@@ -123,7 +125,14 @@ static inline void hostapd_ubus_notify(struct hostapd_data *hapd, const char *ty
 {
 }
 
-static inline void hostapd_ubus_notify_dpp_pb_result(struct hostapd_data *hapd, const char *status)
+static inline void hostapd_ubus_notify_dpp_conf_failed(
+	struct hostapd_data *hapd, const struct dpp_authentication *auth)
+{
+}
+
+static inline void hostapd_ubus_notify_dpp_result(
+	struct hostapd_data *hapd, const char *status,
+	const struct dpp_authentication *auth)
 {
 }
 
diff --git a/essentials/hostapd_s1g/src/wpa_supplicant/ubus.c b/essentials/hostapd_s1g/src/wpa_supplicant/ubus.c
index ee8742f7..963bb025 100644
--- a/wpa_supplicant/ubus.c
+++ b/wpa_supplicant/ubus.c
@@ -29,6 +29,16 @@ static struct ubus_context *ctx;
 static struct blob_buf b;
 static int ctx_ref;
 
+static void
+blobmsg_add_macaddr(struct blob_buf *buf, const char *name, const u8 *addr)
+{
+	char *s;
+
+	s = blobmsg_alloc_string_buffer(buf, name, 20);
+	sprintf(s, MACSTR, MAC2STR(addr));
+	blobmsg_add_string_buffer(buf);
+}
+
 int hostapd_config_s1g_val(const struct hostapd_config *conf) {
 	return 0;
 }
@@ -261,31 +271,49 @@ void wpas_ubus_notify_type(struct wpa_supplicant *wpa_s, const char *type)
 	ubus_notify(ctx, &wpa_s->ubus.obj, type, NULL, -1);
 }
 
-#ifdef CONFIG_DPP3
-void wpas_ubus_notify_dpp_pb_result(struct wpa_supplicant *wpa_s, const char *status)
+static bool wpas_ubus_dpp_is_pb(struct wpa_supplicant *wpa_s)
 {
+#ifdef CONFIG_DPP3
+	return (wpa_s->dpp_pb_time.sec || wpa_s->dpp_pb_time.usec);
+#else
+	return false;
+#endif
+}
+
+#ifdef CONFIG_DPP
+void wpas_ubus_notify_dpp_result(struct wpa_supplicant *wpa_s,
+				 const char *status, const u8 *peer_mac)
+{
+	const char *event;
+
 	blob_buf_init(&b, 0);
 
 	blobmsg_add_string(&b, "ifname", wpa_s->ifname);
 	blobmsg_add_string(&b, "status", status);
+	if (peer_mac)
+		blobmsg_add_macaddr(&b, "peer_mac", peer_mac);
 
-	ubus_send_event(ctx, "dpp.result", b.head);
+	event = wpas_ubus_dpp_is_pb(wpa_s) ? "dpp.pb.result" : "dpp.qr.result";
+	ubus_send_event(ctx, event, b.head);
 }
-#endif
 
-#ifdef CONFIG_DPP
 void wpas_ubus_notify_dpp_conf_failed(struct wpa_supplicant *wpa_s)
 {
+	const char *event;
+
 	blob_buf_init(&b, 0);
 
 	blobmsg_add_string(&b, "ifname", wpa_s->ifname);
 
-	ubus_send_event(ctx, "dpp.conf_failed", b.head);
+	event = wpas_ubus_dpp_is_pb(wpa_s) ? "dpp.pb.conf_failed" :
+		"dpp.qr.conf_failed";
+	ubus_send_event(ctx, event, b.head);
 }
 
 void wpas_ubus_notify_dpp_conf_received(struct wpa_supplicant *wpa_s,
 					const struct dpp_config_obj *conf)
 {
+	const char *event;
 	void *tbl;
 
 	blob_buf_init(&b, 0);
@@ -295,7 +323,9 @@ void wpas_ubus_notify_dpp_conf_received(struct wpa_supplicant *wpa_s,
 	blobmsg_add_json_from_string(&b, conf->conf_obj);
 	blobmsg_close_table(&b, tbl);
 
-	ubus_send_event(ctx, "dpp.conf_received", b.head);
+	event = wpas_ubus_dpp_is_pb(wpa_s) ? "dpp.pb.conf_received" :
+		"dpp.qr.conf_received";
+	ubus_send_event(ctx, event, b.head);
 }
 #endif
 
@@ -386,4 +416,3 @@ void wpas_ubus_event_state(struct wpa_supplicant *wpa_s, const char *state)
 	blob_buf_free(&b);
 	return;
 }
-
diff --git a/essentials/hostapd_s1g/src/wpa_supplicant/ubus.h b/essentials/hostapd_s1g/src/wpa_supplicant/ubus.h
index fa039516..6f478363 100644
--- a/wpa_supplicant/ubus.h
+++ b/wpa_supplicant/ubus.h
@@ -30,10 +30,9 @@ void wpas_ubus_event_state(struct wpa_supplicant *wpa_s, const char *state);
 void wpas_ubus_notify(struct wpa_supplicant *wpa_s, const struct wps_credential *cred);
 #endif
 
-#ifdef CONFIG_DPP3
-void wpas_ubus_notify_dpp_pb_result(struct wpa_supplicant *wpa_s, const char *status);
-#endif
 #ifdef CONFIG_DPP
+void wpas_ubus_notify_dpp_result(struct wpa_supplicant *wpa_s,
+				 const char *status, const u8 *peer_mac);
 void wpas_ubus_notify_dpp_conf_failed(struct wpa_supplicant *wpa_s);
 void wpas_ubus_notify_dpp_conf_received(struct wpa_supplicant *wpa_s, const struct dpp_config_obj *conf);
 #endif
@@ -57,7 +56,13 @@ static inline void wpas_ubus_notify(struct wpa_supplicant *wpa_s, struct wps_cre
 {
 }
 
-static inline void wpas_ubus_notify_dpp_pb_result(struct wpa_supplicant *wpa_s, const char *status)
+static inline void wpas_ubus_notify_dpp_result(struct wpa_supplicant *wpa_s,
+					       const char *status,
+					       const u8 *peer_mac)
+{
+}
+
+static inline void wpas_ubus_notify_dpp_conf_failed(struct wpa_supplicant *wpa_s)
 {
 }
 
-- 
2.34.1

