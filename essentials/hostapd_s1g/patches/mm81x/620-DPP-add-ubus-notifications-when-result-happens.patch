--- a/src/ap/dpp_hostapd.c
+++ b/src/ap/dpp_hostapd.c
@@ -23,6 +23,7 @@
 #include "gas_serv.h"
 #include "wpa_auth.h"
 #include "beacon.h"
+#include "ubus.h"
 #include "dpp_hostapd.h"
 
 #ifdef CONFIG_MORSE_5GHZ_MAPPED
@@ -1385,8 +1386,10 @@ static void hostapd_dpp_gas_resp_cb(void *ctx, const u8 *addr, u8 dialog_token,
 	}
 #endif /* CONFIG_TESTING_OPTIONS */
 fail:
-	if (status != DPP_STATUS_OK)
+	if (status != DPP_STATUS_OK) {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		hostapd_ubus_notify_dpp_conf_failed(hapd, auth);
+	}
 #ifdef CONFIG_DPP2
 	if (auth->peer_version >= 2 &&
 	    auth->conf_resp_status == DPP_STATUS_OK) {
@@ -1585,6 +1588,7 @@ static void hostapd_dpp_config_result_wait_timeout(void *eloop_ctx,
 	wpa_printf(MSG_DEBUG,
 		   "DPP: Timeout while waiting for Configuration Result");
 	wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_FAILED);
+	hostapd_ubus_notify_dpp_conf_failed(hapd, auth);
 	dpp_auth_deinit(auth);
 	hapd->dpp_auth = NULL;
 }
@@ -1691,23 +1695,31 @@ static void hostapd_dpp_rx_conf_result(struct hostapd_data *hapd, const u8 *src,
 	}
 	hostapd_drv_send_action_cancel_wait(hapd);
 	hostapd_dpp_listen_stop(hapd);
-	if (status == DPP_STATUS_OK)
+	if (status == DPP_STATUS_OK) {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_SENT
 			"conf_status=%d", auth->conf_resp_status);
-	else
+		if (auth->peer_bi && auth->peer_bi->type == DPP_BOOTSTRAP_QR_CODE)
+			hostapd_ubus_notify_dpp_result(hapd, "success", auth);
+	} else {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		hostapd_ubus_notify_dpp_conf_failed(hapd, auth);
+	}
 	dpp_auth_deinit(auth);
 	hapd->dpp_auth = NULL;
 	eloop_cancel_timeout(hostapd_dpp_config_result_wait_timeout, hapd,
 			     NULL);
 #ifdef CONFIG_DPP3
 	if (!ifaces->dpp_pb_result_indicated && hostapd_dpp_pb_active(hapd)) {
-		if (status == DPP_STATUS_OK)
+		if (status == DPP_STATUS_OK) {
 			wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_PB_RESULT
 				"success");
-		else
+			hostapd_ubus_notify_dpp_result(hapd, "success", NULL);
+		} else {
 			wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_PB_RESULT
 				"no-configuration-available");
+			hostapd_ubus_notify_dpp_result(
+				hapd, "no-configuration-available", NULL);
+		}
 		ifaces->dpp_pb_result_indicated = true;
 		if (status == DPP_STATUS_OK)
 			hostapd_dpp_remove_pb_hash(hapd);
@@ -1823,6 +1835,9 @@ hostapd_dpp_rx_presence_announcement(struct hostapd_data *hapd, const u8 *src,
 	if (hostapd_dpp_auth_init_next(hapd) < 0) {
 		dpp_auth_deinit(hapd->dpp_auth);
 		hapd->dpp_auth = NULL;
+	} else if (auth->own_bi && auth->own_bi->type == DPP_BOOTSTRAP_QR_CODE) {
+		hostapd_ubus_notify_dpp_result(hapd, "started",
+					       hapd->dpp_auth);
 	}
 }
 
@@ -2729,6 +2744,8 @@ hostapd_dpp_rx_pb_presence_announcement(struct hostapd_data *hapd,
 				wpa_msg(hapd->msg_ctx, MSG_INFO,
 					DPP_EVENT_PB_RESULT "session-overlap");
 				ifaces->dpp_pb_result_indicated = true;
+				hostapd_ubus_notify_dpp_result(
+					hapd, "session-overlap", NULL);
 			}
 			hostapd_dpp_push_button_stop(hapd);
 			return;
@@ -3292,8 +3309,10 @@ hostapd_dpp_gas_req_handler(struct hostapd_data *hapd, const u8 *sa,
 	}
 
 	resp = dpp_conf_req_rx(auth, query, query_len);
-	if (!resp)
+	if (!resp) {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		hostapd_ubus_notify_dpp_conf_failed(hapd, auth);
+	}
 	return resp;
 }
 
@@ -3337,21 +3356,27 @@ void hostapd_dpp_gas_status_handler(struct hostapd_data *hapd, int ok)
 #endif /* CONFIG_DPP2 */
 	hostapd_drv_send_action_cancel_wait(hapd);
 
-	if (ok)
+	if (ok) {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_SENT
 			"conf_status=%d", auth->conf_resp_status);
-	else
+	} else {
 		wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		hostapd_ubus_notify_dpp_conf_failed(hapd, auth);
+	}
 	dpp_auth_deinit(hapd->dpp_auth);
 	hapd->dpp_auth = NULL;
 #ifdef CONFIG_DPP3
 	if (!ifaces->dpp_pb_result_indicated && hostapd_dpp_pb_active(hapd)) {
-		if (ok)
+		if (ok) {
 			wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_PB_RESULT
 				"success");
-		else
+			hostapd_ubus_notify_dpp_result(hapd, "success", NULL);
+		} else {
 			wpa_msg(hapd->msg_ctx, MSG_INFO, DPP_EVENT_PB_RESULT
 				"could-not-connect");
+			hostapd_ubus_notify_dpp_result(
+				hapd, "could-not-connect", NULL);
+		}
 		ifaces->dpp_pb_result_indicated = true;
 		if (ok)
 			hostapd_dpp_remove_pb_hash(hapd);
@@ -4133,9 +4158,11 @@ void hostapd_dpp_push_button_stop(struct hostapd_data *hapd)
 	eloop_cancel_timeout(hostapd_dpp_push_button_expire, hapd, NULL);
 	if (hostapd_dpp_pb_active(hapd)) {
 		wpa_printf(MSG_DEBUG, "DPP: Stop active push button mode");
-		if (!ifaces->dpp_pb_result_indicated)
+		if (!ifaces->dpp_pb_result_indicated) {
 			wpa_msg(hapd->msg_ctx, MSG_INFO,
 				DPP_EVENT_PB_RESULT "failed");
+			hostapd_ubus_notify_dpp_result(hapd, "failed", NULL);
+		}
 	}
 	ifaces->dpp_pb_time.sec = 0;
 	ifaces->dpp_pb_time.usec = 0;
--- a/src/common/dpp.c
+++ b/src/common/dpp.c
@@ -1514,6 +1514,7 @@ void dpp_auth_deinit(struct dpp_authentication *auth)
 		struct dpp_config_obj *conf = &auth->conf_obj[i];
 
 		os_free(conf->connector);
+		os_free(conf->conf_obj);
 		wpabuf_free(conf->c_sign_key);
 		wpabuf_free(conf->certbag);
 		wpabuf_free(conf->certs);
@@ -3309,6 +3310,8 @@ static int dpp_parse_conf_obj(struct dpp_authentication *auth,
 	}
 	conf = &auth->conf_obj[auth->num_conf_obj++];
 
+	conf->conf_obj = os_strdup((const char *) conf_obj);
+
 	if (ssid64) {
 		conf->ssid_len = wpabuf_len(ssid64);
 		os_memcpy(conf->ssid, wpabuf_head(ssid64), conf->ssid_len);
--- a/src/common/dpp.h
+++ b/src/common/dpp.h
@@ -363,6 +363,7 @@ struct dpp_authentication {
 	int provision_configurator;
 	struct dpp_configurator *conf;
 	struct dpp_config_obj {
+		char *conf_obj; /* raw JSON of config object */
 		char *connector; /* received signedConnector */
 		u8 ssid[SSID_MAX_LEN];
 		u8 ssid_len;
--- a/wpa_supplicant/dpp_supplicant.c
+++ b/wpa_supplicant/dpp_supplicant.c
@@ -33,6 +33,7 @@
 #include "scan.h"
 #include "notify.h"
 #include "dpp_supplicant.h"
+#include "ubus.h"
 
 #ifdef CONFIG_DPP2
 #define DEFAULT_CHIRP_FOREVER_START_DELAY_S     (2)
@@ -1379,6 +1380,8 @@ static void wpas_dpp_rx_auth_req(struct wpa_supplicant *wpa_s, const u8 *src,
 		return;
 	}
 	os_memcpy(wpa_s->dpp_auth->peer_mac_addr, src, ETH_ALEN);
+	if (own_bi->type == DPP_BOOTSTRAP_QR_CODE)
+		wpas_ubus_notify_dpp_result(wpa_s, "started", src);
 
 	if (wpa_s->dpp_listen_freq &&
 	    wpa_s->dpp_listen_freq != wpa_s->dpp_auth->curr_freq) {
@@ -1776,6 +1779,8 @@ static int wpas_dpp_handle_config_obj(struct wpa_supplicant *wpa_s,
 				      struct dpp_authentication *auth,
 				      struct dpp_config_obj *conf)
 {
+	wpas_ubus_notify_dpp_conf_received(wpa_s, conf);
+
 	wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_RECEIVED);
 	wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONFOBJ_AKM "%s",
 		dpp_akm_str(conf->akm));
@@ -1895,6 +1900,9 @@ static int wpas_dpp_handle_config_obj(struct wpa_supplicant *wpa_s,
 #ifdef CONFIG_DPP3
 	if (!wpa_s->dpp_pb_result_indicated) {
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT "success");
+		wpas_ubus_notify_dpp_result(
+			wpa_s, "success",
+			wpa_s->dpp_auth ? wpa_s->dpp_auth->peer_mac_addr : NULL);
 		MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 				   .result = MORSE_DPP_PB_RESULT_SUCCESS);
 		wpa_s->dpp_pb_result_indicated = true;
@@ -2069,8 +2077,10 @@ static void wpas_dpp_gas_resp_cb(void *ctx, const u8 *addr, u8 dialog_token,
 	}
 #endif /* CONFIG_TESTING_OPTIONS */
 fail:
-	if (status != DPP_STATUS_OK)
+	if (status != DPP_STATUS_OK) {
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		wpas_ubus_notify_dpp_conf_failed(wpa_s);
+	}
 #ifdef CONFIG_DPP2
 	if (auth->peer_version >= 2 &&
 	    auth->conf_resp_status == DPP_STATUS_OK) {
@@ -2116,6 +2126,7 @@ static void wpas_dpp_gas_client_timeout(void *eloop_ctx, void *timeout_ctx)
 
 	wpa_printf(MSG_DEBUG, "DPP: Timeout while waiting for Config Response");
 	wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+	wpas_ubus_notify_dpp_conf_failed(wpa_s);
 	dpp_auth_deinit(wpa_s->dpp_auth);
 	wpa_s->dpp_auth = NULL;
 }
@@ -2307,6 +2318,7 @@ static void wpas_dpp_config_result_wait_timeout(void *eloop_ctx,
 	wpa_printf(MSG_DEBUG,
 		   "DPP: Timeout while waiting for Configuration Result");
 	wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+	wpas_ubus_notify_dpp_conf_failed(wpa_s);
 	dpp_auth_deinit(auth);
 	wpa_s->dpp_auth = NULL;
 }
@@ -2431,8 +2443,10 @@ static void wpas_dpp_rx_conf_result(struct wpa_supplicant *wpa_s, const u8 *src,
 	if (status == DPP_STATUS_OK)
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_SENT "conf_status=%d",
 			auth->conf_resp_status);
-	else
+	else {
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		wpas_ubus_notify_dpp_conf_failed(wpa_s);
+	}
 	dpp_auth_deinit(auth);
 	wpa_s->dpp_auth = NULL;
 	eloop_cancel_timeout(wpas_dpp_config_result_wait_timeout, wpa_s, NULL);
@@ -2441,11 +2455,14 @@ static void wpas_dpp_rx_conf_result(struct wpa_supplicant *wpa_s, const u8 *src,
 		if (status == DPP_STATUS_OK) {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT
 				"success");
+			wpas_ubus_notify_dpp_result(wpa_s, "success", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_SUCCESS);
 		} else {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT
 				"no-configuration-available");
+			wpas_ubus_notify_dpp_result(
+				wpa_s, "no-configuration-available", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_NO_CONFIG);
 		}
@@ -3806,6 +3823,8 @@ wpas_dpp_rx_pb_presence_announcement(struct wpa_supplicant *wpa_s,
 			    wpas_dpp_pb_active(wpa_s)) {
 				wpa_msg(wpa_s, MSG_INFO,
 					DPP_EVENT_PB_RESULT "session-overlap");
+				wpas_ubus_notify_dpp_result(
+					wpa_s, "session-overlap", NULL);
 				MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 						   .result = MORSE_DPP_PB_RESULT_SESSION_OVERLAP,
 	);
@@ -3963,6 +3982,7 @@ skip_hash_check:
 		if (!wpa_s->dpp_pb_result_indicated) {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT
 				"session-overlap");
+			wpas_ubus_notify_dpp_result(wpa_s, "session-overlap", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_SESSION_OVERLAP);
 			wpa_s->dpp_pb_result_indicated = true;
@@ -4529,6 +4549,7 @@ wpas_dpp_gas_req_handler(void *ctx, void *resp_ctx, const u8 *sa,
 	auth->conf_resp = resp;
 	if (!resp) {
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		wpas_ubus_notify_dpp_conf_failed(wpa_s);
 		dpp_auth_deinit(wpa_s->dpp_auth);
 		wpa_s->dpp_auth = NULL;
 	}
@@ -4596,8 +4617,10 @@ wpas_dpp_gas_status_handler(void *ctx, struct wpabuf *resp, int ok)
 	if (ok)
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_SENT "conf_status=%d",
 			auth->conf_resp_status);
-	else
+	else {
 		wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_CONF_FAILED);
+		wpas_ubus_notify_dpp_conf_failed(wpa_s);
+	}
 	dpp_auth_deinit(wpa_s->dpp_auth);
 	wpa_s->dpp_auth = NULL;
 	wpabuf_free(resp);
@@ -4606,11 +4629,14 @@ wpas_dpp_gas_status_handler(void *ctx, struct wpabuf *resp, int ok)
 		if (ok) {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT
 				"success");
+			wpas_ubus_notify_dpp_result(wpa_s, "success", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_SUCCESS);
 		} else {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT
 				"could-not-connect");
+			wpas_ubus_notify_dpp_result(
+				wpa_s, "could-not-connect", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_COULD_NOT_CONNECT);
 		}
@@ -6214,6 +6240,7 @@ void wpas_dpp_push_button_stop(struct wpa_supplicant *wpa_s)
 		wpa_s->dpp_pb_bi = NULL;
 		if (!wpa_s->dpp_pb_result_indicated) {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT "failed");
+			wpas_ubus_notify_dpp_result(wpa_s, "failed", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_FAILED);
 			wpa_s->dpp_pb_result_indicated = true;
@@ -6234,6 +6261,7 @@ void wpas_dpp_push_button_stop(struct wpa_supplicant *wpa_s)
 		wpa_printf(MSG_DEBUG, "DPP: Stop active push button mode");
 		if (!wpa_s->dpp_pb_result_indicated) {
 			wpa_msg(wpa_s, MSG_INFO, DPP_EVENT_PB_RESULT "failed");
+			wpas_ubus_notify_dpp_result(wpa_s, "failed", NULL);
 			MORSE_DPP_EVT_CALL(MORSE_DPP_EVT_PB_RESULT, pb_result,
 					   .result = MORSE_DPP_PB_RESULT_FAILED);
 		}
